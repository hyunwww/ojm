<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.ojm.mapper.ReviewAttachMapper">
	<!--
	<delete id='delete'>
		delete from reviewimg where uuid = #{uuid}
	</delete>
	<delete id="deleteAll">
		delete reviewimg where rvino = #{rvino}
	</delete>
	<insert id='addrvimg' parameterType="org.ojm.domain.ReviewAttachVO">
		insert into reviewimg (rvino, rvno, uuid, uploadPath, fileName)
		values(
		seq_rvino.nextval,
		#{rvno},
		#{uuid}, 
		#{uploadPath}, 
		#{fileName})
	</insert>
	<select id='findByRvno' resultType = "org.ojm.domain.ReviewAttachVO">
		select rvno from reviewimg where rvino = #{rvino}
	</select>
	-->
</mapper>